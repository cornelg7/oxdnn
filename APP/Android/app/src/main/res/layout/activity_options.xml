<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="oxfordteam5.DeeplyDecodedDisability.OptionsActivity">

    <CheckBox
        android:id="@+id/checkBoxSave"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:onClick="ChangeSave"
        android:text="Save pictures in the gallery"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.15" />


    <CheckBox
        android:id="@+id/checkBoxFocusCamera"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:layout_marginEnd="8dp"
        android:onClick="ChangeFocus"
        android:text="Focus the camera when taking instant picture (slower)"
        app:layout_constraintStart_toStartOf="@+id/checkBoxSave"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/checkBoxSave"/>

    <CheckBox
        android:id="@+id/checkBoxRotateBitmap"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:layout_marginEnd="8dp"
        android:onClick="ChangeRotate"
        android:text="Allow app to rotate pictures when displaying them to better fit the screen"
        app:layout_constraintStart_toStartOf="@+id/checkBoxSave"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/checkBoxFocusCamera" />

</android.support.constraint.ConstraintLayout>
